# Scriber stores your ElevenLabs API key in ~/.scriber/config.json via the
# first-run modal in the GUI — no env vars needed for normal use.
#
# Optional override: point Scriber at a custom data directory instead of
# ~/.scriber/. Useful for testing or running multiple isolated instances.
# SCRIBER_HOME=/path/to/custom/scriber/home
